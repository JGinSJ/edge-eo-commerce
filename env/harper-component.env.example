# harper-prerender component env (set in the Harper instance/component env).
# Both optional — leave empty to run the bot-key gate OPEN (dev only).
BOT_REQUEST_KEY_NAME=x-pr-req-key
BOT_REQUEST_KEY=<shared-secret>   # MUST equal the EW's PMUSER_HARPER_BOT_KEY, or /page & /page_content 401
